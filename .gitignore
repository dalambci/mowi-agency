node_modules/
screenshots/
blog-preview/

# ralph-loop plugin state (iteration counter + promise), rewritten every iteration
.claude/ralph-loop.local.md

# ralph loop run transcripts - useful for debugging a run, not worth committing
ralph/logs/

# Build output + render scratch from the social-content workstream (parked,
# uncommitted on master as of 2026-09-02). Ignored here so the loop's paths
# gate does not see them as dirt.
out/
content/social-templates/.render-tmp.html
